import { print } from "bolt:stdio"

type Person = {
    name: String,
    age: Integer,
    active: Bool
}

val alice := Person {
    name: "Alice",
    age: 30,
    active: true
}

val bob := Person {
    name: "Bob", 
    age: 25,
    active: false
}

print("Struct instances created")